<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.AD_ID" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".ShortsApplication"
        android:theme="@style/Theme.Shorts">
        <activity android:name=".ui.activity.PlayerActivity"/>
        <activity android:name=".ui.activity.ConsumeRecordActivity"/>
        <activity android:name=".ui.activity.TopUpRecordActivity"/>
        <activity android:name=".ui.activity.VipActivity"/>
        <activity android:name=".ui.activity.WalletActivity"/>
        <activity android:name=".ui.activity.ShopActivity"/>
        <activity android:name=".ui.activity.WebViewActivity"/>
        <activity android:name=".ui.activity.SettingsActivity"/>
        <activity android:name=".ui.activity.HistoryActivity"/>
        <activity android:name=".ui.activity.FeedbackActivity"/>
        <activity android:name=".ui.activity.DramaEpisodesActivity" android:theme="@style/ShortsTranslucent"/>
        <activity android:name=".ui.activity.MainActivity" android:launchMode="singleTask"/>
        <activity
            android:name=".ui.activity.SplashActivity"
            android:exported="true"
            android:theme="@style/FastStartTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>